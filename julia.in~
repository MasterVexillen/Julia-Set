# size (px) of canvas
900 600

# plot range (Re_ctr, Im_ctr, Re_range) --- Im_range will be determined from size of canvas automatically
0 0 4

# max_iter, escape
5000 75

# Hue scaling, saturation osc. freq., value osc. freq., hue offset(deg)
1 12 12 0

# C (Re, Im)
.268 .06